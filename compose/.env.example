# Copy this file to .env and fill in your values.

# IP address of the DGX Spark running Ollama on port 11434
DGX_SPARK_IP=10.10.12.251

# Hostname/IP of this XPS node (used as SearXNG base URL)
XPS_IP=10.10.12.252

# SearXNG secret key — generate with: openssl rand -hex 32
SEARXNG_SECRET_KEY=changeme-run-openssl-rand-hex-32
