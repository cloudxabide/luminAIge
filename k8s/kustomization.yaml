apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - configmap.yaml
  - secret.yaml       # copy from secret.example.yaml and fill in real values
  - openwebui.yaml
  - qdrant.yaml
  - searxng.yaml
  - nemoclaw.yaml
